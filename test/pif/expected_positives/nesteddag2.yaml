---
parsed:
  :value:
    exercise_id: https://github.com/CSSPLICE/peml-feasibility-examples/blob/main/parsons/nesteddag2.peml
    title: 'Nested DAG Demo: Implicit DAG'
    author: Cliff Shaffer
    license:
      id: MIT
      owner:
        name: Cliff Shaffer
        email: shaffer@vt.edu
    tags:
      topics: PEML Demo Parsons Problem
      style: parsons, order
    instructions: |
      <p>Associate each test with its corresponding work, but you can do the<br />
      tests in any order.</p>
    systems:
    - language: java
      assets:
        code:
          blocks:
            content:
            - blockid: testAblock
              depends: ''
              blocklist:
              - blockid: caseA
                display: if (testA) {
                depends: ''
              - blockid: stuffA
                display: |2
                    doAstuff();
                  }
                depends: caseA
            - blockid: testBblock
              depends: ''
              blocklist:
              - blockid: caseB
                display: if (testB) {
                depends: ''
              - blockid: stuffB
                display: |2
                    doBstuff();
                  }
                depends: caseB
            - blockid: testCblock
              depends: ''
              blocklist:
              - blockid: caseC
                display: if (testC) {
                depends: ''
              - blockid: stuffC
                display: |2
                    doCstuff();
                  }
                depends: caseC
  :diagnostics: []
renderable:
  value:
    question_text: |
      <p>Associate each test with its corresponding work, but you can do the<br />
      tests in any order.</p>
    options:
      grader:
        type: dag
        showFeedback: true
      maxdist: 0
      order: ''
      indent: false
      adaptive: true
      numbered: false
      language: java
      runnable: true
    blocks: []
  diags:
  - 'Parsons does not support nontrivial blocklists: {"blockid"=>"testAblock", "depends"=>"",
    "blocklist"=>[{"blockid"=>"caseA", "display"=>"if (testA) {", "depends"=>""},
    {"blockid"=>"stuffA", "display"=>"  doAstuff();\n}\n", "depends"=>"caseA"}]}.'
  - 'Parsons does not support nontrivial blocklists: {"blockid"=>"testBblock", "depends"=>"",
    "blocklist"=>[{"blockid"=>"caseB", "display"=>"if (testB) {", "depends"=>""},
    {"blockid"=>"stuffB", "display"=>"  doBstuff();\n}\n", "depends"=>"caseB"}]}.'
  - 'Parsons does not support nontrivial blocklists: {"blockid"=>"testCblock", "depends"=>"",
    "blocklist"=>[{"blockid"=>"caseC", "display"=>"if (testC) {", "depends"=>""},
    {"blockid"=>"stuffC", "display"=>"  doCstuff();\n}\n", "depends"=>"caseC"}]}.'
