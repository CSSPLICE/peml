---
parsed:
  :value:
    exercise_id: https://github.com/CSSPLICE/peml-feasibility-examples/blob/main/parsons/swap1-execute.peml
    title: Swap1_execute_PEML
    license:
      id: MIT
      owner:
        name: Cliff Shaffer
        email: shaffer@vt.edu
    tags:
      topics: 'PEML Demo Parsons Problem: Swap'
    settings:
      grader:
        type: execute
      indent:
        active: 'true'
        mode: free
        max_indents: '3'
    instructions: |
      <p>Write a function to swap two elements in the array by reordering the<br />
      lines. Your function should return the array.</p>
    systems:
    - language: Java
      assets:
        code:
          wrapper:
            files:
            - content: |
                public class Answer
                {
                    ___
                }
          blocks:
            content:
            - tag: fixed
              display: |2
                    return arr;
                }
              blockid: fixed
        test:
          files:
          - type: text/csv-unquoted
            pattern:
              method_invocation: Swap({{arr}}, {{i}}, {{j}})
            content: |
              arr, i, j, expected, description
              new int[]{1, 2, 3, 4}, 2, 3, new int[]{1,2,4,3}, example
              new int[]{1, 2, 3, 4}, 0, 3, new int[]{4,2,3,1}, hidden
  :diagnostics: []
renderable:
  value:
    question_text: |
      <p>Write a function to swap two elements in the array by reordering the<br />
      lines. Your function should return the array.</p>
    options:
      grader:
        type: exec
        show_feedback: true
      indent:
        active: true
        mode: free
        max_indents: 3
      adaptive: true
      numbered: false
      language: java
    blocks:
    - text: |2
            return arr;
        }
      type: ''
      tag: fixed
      depends: ''
      indent: true
      displaymath: false
      feedback: ''
  diags: []
