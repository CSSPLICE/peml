---
parsed:
  :value:
    exercise_id: https://github.com/CSSPLICE/peml-feasibility-examples/blob/main/parsons/swap1-execute.peml
    title: Swap1_execute_PEML
    license:
      id: MIT
      owner:
        name: Cliff Shaffer
        email: shaffer@vt.edu
    tags:
      topics: 'PEML Demo Parsons Problem: Swap'
      style: parsons, execute
    instructions: |
      Write a function to swap two elements in the array by reordering the
      lines. Your function should return the array.
    systems:
    - language: Java
      assets:
        code:
          wrapper:
            files:
            - content: |
                public class Answer
                {
                    ___
                }
          blocks:
            content:
            - tag: fixed
              display: |2
                    return arr;
                }
              blockid: fixed
        test:
          files:
          - type: text/csv-unquoted
            pattern:
              method_invocation: Swap({{arr}}, {{i}}, {{j}})
            content: |
              arr, i, j, expected, description
              new int[]{1, 2, 3, 4}, 2, 3, new int[]{1,2,4,3}, example
              new int[]{1, 2, 3, 4}, 0, 3, new int[]{4,2,3,1}, hidden
  :diagnostics:
  - 'The document is missing key: assets'
runestone:
  value:
    question_text: |
      Write a function to swap two elements in the array by reordering the
      lines. Your function should return the array.
    options:
      grader:
        type: exec
        showFeedback: true
      maxdist: 0
      order: ''
      indent: false
      adaptive: true
      numbered: false
      language: java
      runnable: true
    blocks:
    - text: |2
            return arr;
        }
      type: ''
      tag: fixed
      depends: ''
      indent: ''
      displaymath: true
  diags: []
