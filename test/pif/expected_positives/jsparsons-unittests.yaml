---
parsed:
  :value:
    exercise_id: JSParsons Unittests
    title: JSParsons Unittests
    license:
      id: MIT
      owner:
        name: Cliff Shaffer
        email: shaffer@vt.edu
    tags:
      topics: PEML Demo Parsons Problem
      style: parsons, execute, indent
    instructions: 'Your task is to construct a function which returns the index of
      the largest element in the array.

      '
    systems:
    - language: Python
      assets:
        code:
          wrapper:
            files:
            - content: "    ___\n"
          blocks:
            content:
            - blockid: one
              display: 'def maxindex(arg):'
              indent: '0'
            - blockid: two
              display: ans = 0
              indent: '1'
            - blockid: three
              display: 'for i in range(len(arg)):'
              indent: '1'
            - blockid: four
              display: 'if arg[i] > arg[ans]:'
              indent: '2'
            - blockid: five
              display: ans = i
              indent: '3'
            - blockid: six
              display: return ans
              indent: '1'
        test:
          files:
          - type: |
              text/csv-unquoted
                maxindex(arr)
            content: |
              arr, expected, description
              [0, 2, 4], 2, example
              [7, 2, 4], 0, hidden
              [7, 8, 4], 1, example
  :diagnostics: &1
  - Path '/exercise_id' does not match the required pattern (^[^\s,]+$)
  - 'Path ''/systems/0/assets/test/files/0'' is missing key: format'
renderable: *1
