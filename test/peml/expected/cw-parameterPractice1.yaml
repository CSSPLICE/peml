---
:value:
  exercise_id: CS1114_Week2SyntaxDrill1
  title: CodeWorkout Parameter Practice 1
  vendor:
    codeworkout:
      is_public: 'true'
  difficulty: '50'
  license:
    id: cc-sa-4.0
    owner:
      email: admin@codeworkout.org
      name: codeworkout
  tags:
    topics: CS1114, Parameters, Methods, java
    style: code-writing
  instructions: "Assume you have access to the following method.\n\n    public int
    add3Ints(int a, int b, int c){\n              return a + b + c;\n            }\n
    \           \n\nCall `add3Ints` and pass the numbers 4, 10, and 9 as parameters.\n"
  systems:
  - language: Java
    assets:
      code:
        wrapper:
          files:
          - content: |
              public class Syntax1to3 {
                  private int aVal;
                  private int bVal;
                  private int cVal;

                  public int add3Ints(int a, int b, int c){
                      aVal = a;
                      bVal = b;
                      cVal = c;
                      return a + b + c;
                    }

                  public void run() {
                    ___
                  }

                  public int getA() {
                      return aVal;
                  }

                  public int getB() {
                      return bVal;
                  }

                  public int getC() {
                      return cVal;
                  }

              }
        starter:
          files:
          - content: '___

              '
      test:
        files:
        - type: java/junit
          content: |
            import codeworkout.CodeWorkoutTest;
            public class Syntax1to3Test extends CodeWorkoutTest {

              public Syntax1to3 test;
              public void setUp() {
                  test = new Syntax1to3();
              }

              public void testParametersSetCorrectly() {
                  int aVal = 4;
                  int bVal = 10;
                  int cVal = 9;

                  test.run();

                  assertEquals(aVal + "should be the first parameter passed!", aVal, test.getA());
                  assertEquals(bVal + "should be the second parameter passed!", bVal, test.getB());
                  assertEquals(cVal + "should be the third parameter passed!", cVal, test.getC());
              }
            }
:diagnostics:
- Path '/difficulty' is not an integer.
