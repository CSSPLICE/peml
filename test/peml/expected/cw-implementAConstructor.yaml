---
:value:
  exercise_id: ITSC1213_constructor_2
  title: Implement a Constructor
  vendor:
    codeworkout:
      is_public: 'false'
  difficulty: '10'
  license:
    id: cc-sa-4.0
    owner:
      email: smcleo12@uncc.edu
      name: smcleo12
  tags:
    topics: constructors
    style: code writing
  instructions: 'Given the following constructor, set the fields to the corresponding
    parameters.

    '
  systems:
  - language: Java
    assets:
      code:
        wrapper:
          files:
          - content: "___ \n"
        starter:
          files:
          - content: |
              public class Course
              {
                 public int number;
                 public String title;
                 public String department;
                 public int capacity;

                 public Course(int num, String t, String dept, int cap)
                 {
                   ___
                 }
              }
      test:
        files:
        - type: java/junit
          content: |
            import static org.junit.Assert.*;
            import org.junit.*;
            import codeworkout.*;
            import java.lang.reflect.Constructor;
            import java.lang.reflect.Type;

            public class CourseTest extends CodeWorkoutTest
            {
                @Before
                public void setUp()
                {
                }

               @Description("Checking that the fields are set correctly")
               @Test
               public void testConstructorExercise()
               {
                 boolean correctValues = false;
                 Course c = new Course(1213, Intro to CS II, ITSC, 72);
                 correctValues = (c.number == 1213 && "Intro to CS II".equals(c.title) && "ITSC".equals(c.department) && c.capacity == 72);
                 assertTrue("Fields were not set correctly", correctValues);
               }
            }
:diagnostics:
- Path '/difficulty' is not an integer.
