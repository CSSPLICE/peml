---
:value:
  exercise_id: cw-x40
  title: Arrays - Expand Capacity
  vendor:
    codeworkout:
      is_public: 'true'
  difficulty: '10'
  license:
    id: cc-sa-4.0
    owner:
      email: edwards@cs.vt.edu
      name: Stephen Edwards
  tags:
    topics: array, int
    style: coding problem
  instructions: |
    Return an array with the same contents, but double the size of the array
    passed in. The new slots will be filled with zeros. Use the built in array
    functionality that is provided by Java.
  systems:
  - language: Java
    assets:
      code:
        wrapper:
          files:
          - content: |
              import java.util.Arrays;
              public class Answer
              {
                ___
              }
        starter:
          files:
          - content: |
              public int[] expandCapacity(int[] arr)
              {
                  ___
              }
      test:
        files:
        - type: text/yaml
          pattern:
            method_invocation: expandCapacity({{array}})
          content: |
            - array: "new int[]{2, 3, 0}"
              expected: "new int[]{2, 3, 0, 0, 0, 0}"
              description: example
            - array: "new int[]{}"
              expected: "new int[]{}"
              description: example
            - array: "new int[]{6}"
              expected: "new int[]{6, 0}"
              description: example
            - array: "new int[]{9, 9, 1}"
              expected: "new int[]{9, 9, 1, 0, 0, 0}"
            - array: "new int[]{5, 5}"
              expected: "new int[]{5, 5, 0, 0}"
            - array: "new int[]{10, 1, 1, 1, 1}"
              expected: "new int[]{10, 1, 1, 1, 1, 0, 0, 0, 0, 0}"
              description: hidden
            - array: "new int[]{7, 8}"
              expected: "new int[]{7, 8, 0, 0}"
              description: hidden
:diagnostics:
- Path '/difficulty' is not an integer.
