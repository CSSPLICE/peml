---
:value:
  exercise_id: RecC-Multiply
  title: 'Recursion Programming Exercise: Multiply'
  vendor:
    codeworkout:
      is_public: 'true'
  difficulty: '10'
  license:
    id: cc-sa-4.0
    owner:
      email: sallyhamouda@gmail.com
      name: sallyhamouda
  tags:
    topics: recursion
    style: code writing
  instructions: |
    For function `multiply`, write the missing base case
    condition and action. This function will multiply two
    numbers `x` and `y`. You can assume that both `x` and
    `y` are positive.

    If `x` equals one then there is only one `y`.
  systems:
  - language: Java
    assets:
      code:
        wrapper:
          files:
          - content: |
              public class  RecCMultiply
              {
                  ___
              }
        starter:
          files:
          - content: |
              public int multiply(int x, int y) {
                  if <<Missing base case condition>> {
                    <<Missing base case action>>
                  } else {
                    return multiply(x - 1, y) + y;
                  }
              }
      test:
        files:
        - type: text/csv-unquoted
          pattern:
            method_invocation: multiply({{x}}, {{y}})
          content: |
            x, y, expected, description
            2, 3, 6, example
            4, 4, 16
            9, 10, 90
            7, 15, 105
            1, 1, 1, hidden
:diagnostics:
- Path '/difficulty' is not an integer.
