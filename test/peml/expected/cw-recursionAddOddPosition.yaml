---
value:
  exercise_id: RecC-Addodd
  title: 'Recursion Programming Exercise: Add odd values'
  vendor:
    codeworkout:
      is_public: 'true'
  difficulty: '10'
  license:
    id: cc-sa-4.0
    owner:
      email: sallyhamouda@gmail.com
      name: sallyhamouda
  tags:
    topics: recursion
    style: code writing
  instructions: |
    For function `addOdd(n)` write the missing recursive call.
    This function should return the sum of all postive odd
    numbers less than or equal to `n`.
  systems:
  - language: Java
    assets:
      code:
        wrapper:
          files:
          - content: |
              public class RecCAddodd
              {
                  ___
              }
        starter:
          files:
          - content: |
              public int addOdd(int n) {
                  if (n <= 0) {
                    return 0;
                  }
                  if (n % 2 != 0) { // Odd value
                    return <<Missing a Recursive call>>
                  } else { // Even value
                    return addOdd(n - 1);
                  }
              }
      test:
        files:
        - type: text/csv-unquoted
          pattern:
            method_invocation: addOdd({{n}})
          content: |
            n, expected, description
            1, 1, example
            2, 1, example
            3, 4, example
            7, 16, example
            8, 16
            9, 25
            10, 25
            11, 36
            12, 36, hidden
            0, 0, hidden
diagnostics:
- Path '/difficulty' is not an integer.
