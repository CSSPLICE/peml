---
value:
  exercise_id: ITSC1213_is_unique_string
  title: isUniqueString
  vendor:
    codeworkout:
      is_public: 'true'
  difficulty: '10'
  license:
    id: cc-sa-4.0
    owner:
      email: lcao2@uncc.edu
      name: lcao2
  tags:
    topics: String, loop
    style: coding problem
  instructions: |
    This method accepts a String object as a parameter. It returns true
    if the String has all unique characters, false otherwise. For example, if
    the String is "coding", the method returns true; if the String is "Lambda",
    the method returns false. The method should return true if the the length
    of the String is less than 1.
  systems:
  - language: Java
    assets:
      code:
        wrapper:
          files:
          - content: |
              public class StringProblem
              {
                 ___
              }
        starter:
          files:
          - content: |
              public boolean isUniqueString(String message)
              {
                 ___
              }
      test:
        files:
        - type: text/csv-unquoted
          pattern:
            method_invocation: isUniqueString({{message}})
          content: |
            message, expected, description
            "coding", true, example
            "Lambda", false, example
            "Hlamnh", true
            "ab cd ef", false
            "", true
            "hello world", false, hidden
diagnostics:
- Path '/difficulty' is not an integer.
