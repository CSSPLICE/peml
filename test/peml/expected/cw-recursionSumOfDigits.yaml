---
:value:
  exercise_id: RecC-SumOfDigits
  title: 'Recursion Programming Exercise: Sum of the Digits'
  vendor:
    codeworkout:
      is_public: 'true'
  difficulty: '10'
  license:
    id: cc-sa-4.0
    owner:
      email: sallyhamouda@gmail.com
      name: sallyhamouda
  tags:
    topics: recursion
    style: code writing
  instructions: |
    For function `sumOfDigits`, write the missing recursive
    call. This function takes a non-negative integer and
    returns the sum of its digits.
  systems:
  - language: Java
    assets:
      code:
        wrapper:
          files:
          - content: |
              public class  RecCSumOfDigits
              {
                  ___
              }
        starter:
          files:
          - content: |
              public int sumOfDigits(int number) {
                  if (number < 10)
                    return number;
                  return <<Missing a Recursive case action>>
              }
      test:
        files:
        - type: text/csv-unquoted
          pattern:
            method_invocation: addOdd({{n}})
          content: |
            n, expected, description
            1234, 10, example
            5678, 26
            0, 0
            10000, 1
            12, 3, hidden
            100002, 3, hidden
:diagnostics:
- Path '/difficulty' is not an integer.
