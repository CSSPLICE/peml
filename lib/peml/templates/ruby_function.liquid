def test{{id}}:
        {% for given in givens %}
            {{ given  }}
        {% endfor %}

        {% for when in whens %}
            {{ when }}
        {% endfor %}

            {{ then }}
end